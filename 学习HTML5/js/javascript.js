document.write("这是一个独立的JS文件");
alert("你是🐖吗？");

//JavaScript语句是发给浏览器的命令。这些命令的作用是告诉浏览器要做的事情.

//需要在html中调用才会执行
function alert()
{
	var mynum=2;
	if (mynum>1) {
		alert("答对了");
	}
}